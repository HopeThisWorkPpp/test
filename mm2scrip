local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Window = Rayfield:CreateWindow({
    Name = "MM2 Enhanced",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "Made for Mobile & PC",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "MM2Script",
        FileName = "Settings"
    }
})

local MainTab = Window:CreateTab("Main")
local MovementTab = Window:CreateTab("Movement")
local SettingsTab = Window:CreateTab("Settings")

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera

-- ===== VARIABLES =====
local prediction = 0.185
local verticalOffset = 0.8
local isMobile = UserInputService.TouchEnabled
local mobileGUI = nil
local buttonFrame = nil

-- ESP Variables
local espEnabled = false
local espObjects = {}
local gunESPEnabled = false
local gunESPObjects = {}

-- Auto-shoot variables
local autoShoot = false
local fovRadius = 300

-- Movement variables
local jumpPower = 50
local walkSpeed = 16
local jumpEnabled = false
local speedEnabled = false
local originalJumpPower = 50
local originalWalkSpeed = 16

-- ===== FUNCTIONS =====

local function GetPlayerRole(player)
    if not player or not player.Character then return "Unknown" end
    
    if player.Backpack:FindFirstChild("Knife") or (player.Character and player.Character:FindFirstChild("Knife")) then
        return "Murderer"
    end
    
    if player.Backpack:FindFirstChild("Gun") or (player.Character and player.Character:FindFirstChild("Gun")) then
        return "Sheriff"
    end
    
    local hasWeapon = false
    for _, tool in pairs(player.Backpack:GetChildren()) do
        if tool:IsA("Tool") then
            hasWeapon = true
            break
        end
    end
    if not hasWeapon then
        for _, tool in pairs(player.Character:GetChildren()) do
            if tool:IsA("Tool") then
                hasWeapon = true
                break
            end
        end
    end
    
    return hasWeapon and "Unknown" or "Innocent"
end

local function GetRoleColor(role)
    if role == "Murderer" then return Color3.fromRGB(255, 0, 0)
    elseif role == "Sheriff" then return Color3.fromRGB(0, 150, 255)
    elseif role == "Innocent" then return Color3.fromRGB(0, 255, 0)
    else return Color3.fromRGB(255, 255, 255) end
end

-- ===== GLOW ESP FUNCTIONS =====
local function CreateGlowESP(player)
    if not player or player == LocalPlayer or not player.Character then return end
    
    local character = player.Character
    local role = GetPlayerRole(player)
    local color = GetRoleColor(role)
    
    local highlight = Instance.new("Highlight")
    highlight.Name = "GlowESP_" .. player.Name
    highlight.Adornee = character
    highlight.FillColor = color
    highlight.FillTransparency = 0.5
    highlight.OutlineColor = color
    highlight.OutlineTransparency = 0.1
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Parent = character
    
    table.insert(espObjects, {
        Player = player,
        Highlight = highlight,
        Color = color
    })
end

local function UpdateGlowESP()
    if not espEnabled then return end
    
    for _, esp in pairs(espObjects) do
        local player = esp.Player
        if not player or not player.Character then
            if esp.Highlight then esp.Highlight:Destroy() end
            continue
        end
        
        local role = GetPlayerRole(player)
        local color = GetRoleColor(role)
        
        if esp.Highlight then
            esp.Highlight.FillColor = color
            esp.Highlight.OutlineColor = color
            esp.Color = color
        end
    end
end

local function RemoveGlowESP(player)
    for i, esp in pairs(espObjects) do
        if esp.Player == player then
            if esp.Highlight then esp.Highlight:Destroy() end
            table.remove(espObjects, i)
            break
        end
    end
end

-- ===== GUN ESP FUNCTIONS (FIXED - Looks in all places) =====
local function CreateGunESP(tool)
    if not tool then return end
    if tool.Name ~= "Gun" then return end
    
    -- Check if gun already has ESP
    for _, esp in pairs(gunESPObjects) do
        if esp.Tool == tool then
            return
        end
    end
    
    -- Check if gun is held by a player
    local isHeld = false
    
    -- Check if parent is a character
    if tool.Parent then
        if tool.Parent:IsA("Model") and tool.Parent:FindFirstChild("Humanoid") then
            isHeld = true
        end
        if tool.Parent:IsA("Backpack") then
            isHeld = true
        end
        -- Check if parent's parent is a character (for tools in models)
        if tool.Parent.Parent and tool.Parent.Parent:IsA("Model") and tool.Parent.Parent:FindFirstChild("Humanoid") then
            isHeld = true
        end
    end
    
    -- Only create ESP if gun is NOT held
    if not isHeld then
        local billboard = Instance.new("BillboardGui")
        billboard.Name = "GunESP"
        billboard.Size = UDim2.new(0, 150, 0, 50)
        billboard.Adornee = tool
        billboard.AlwaysOnTop = true
        billboard.StudsOffset = Vector3.new(0, 2.5, 0)
        billboard.MaxDistance = 200
        billboard.Parent = tool
        
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 1, 0)
        frame.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
        frame.BackgroundTransparency = 0.15
        frame.BorderSizePixel = 3
        frame.BorderColor3 = Color3.fromRGB(255, 255, 255)
        frame.Parent = billboard
        
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 10)
        corner.Parent = frame
        
        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(1, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.Text = "🔫 GUN DROPPED!"
        label.TextSize = 18
        label.TextColor3 = Color3.fromRGB(255, 255, 255)
        label.Font = Enum.Font.SourceSansBold
        label.TextStrokeTransparency = 0.1
        label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        label.Parent = frame
        
        -- Distance text
        local distanceLabel = Instance.new("TextLabel")
        distanceLabel.Size = UDim2.new(1, 0, 0, 20)
        distanceLabel.Position = UDim2.new(0, 0, 1.2, 0)
        distanceLabel.BackgroundTransparency = 1
        distanceLabel.Text = "0 studs"
        distanceLabel.TextSize = 12
        distanceLabel.TextColor3 = Color3.fromRGB(255, 255, 200)
        distanceLabel.Font = Enum.Font.SourceSans
        distanceLabel.TextStrokeTransparency = 0.3
        distanceLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        distanceLabel.Parent = billboard
        
        table.insert(gunESPObjects, {
            Tool = tool,
            Billboard = billboard,
            Frame = frame,
            Label = label,
            DistanceLabel = distanceLabel
        })
        
        -- Animate the glow and update distance
        spawn(function()
            local startTime = tick()
            while billboard and billboard.Parent do
                local elapsed = tick() - startTime
                local pulse = 0.5 + 0.5 * math.sin(elapsed * 2)
                
                if frame then
                    frame.BackgroundTransparency = 0.1 + (0.2 * pulse)
                    frame.BorderColor3 = Color3.fromRGB(255, 200 + (55 * pulse), 0)
                end
                
                -- Update distance
                if distanceLabel and LocalPlayer.Character then
                    local myPos = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if myPos and tool then
                        local distance = (myPos.Position - tool.Position).Magnitude
                        distanceLabel.Text = math.floor(distance) .. " studs"
                    end
                end
                
                task.wait(0.05)
            end
        end)
        
        print("🔫 Gun ESP created for: " .. tool.Name)
    end
end

local function RemoveGunESP(tool)
    for i, esp in pairs(gunESPObjects) do
        if esp.Tool == tool then
            if esp.Billboard then esp.Billboard:Destroy() end
            table.remove(gunESPObjects, i)
            break
        end
    end
end

-- SCAN FOR DROPPED GUNS - Looks EVERYWHERE
local function ScanForDroppedGuns()
    if not gunESPEnabled then return end
    
    -- Search ALL descendants of workspace (everything)
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("Tool") and obj.Name == "Gun" then
            -- Check if this gun is held by a player
            local isHeld = false
            
            -- Check all players
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Gun") == obj then
                    isHeld = true
                    break
                end
                if player.Backpack and player.Backpack:FindFirstChild("Gun") == obj then
                    isHeld = true
                    break
                end
            end
            
            -- Also check if parent is a character directly
            if obj.Parent then
                if obj.Parent:IsA("Model") and obj.Parent:FindFirstChild("Humanoid") then
                    isHeld = true
                end
                if obj.Parent:IsA("Backpack") then
                    isHeld = true
                end
            end
            
            if not isHeld then
                local hasESP = false
                for _, esp in pairs(gunESPObjects) do
                    if esp.Tool == obj then
                        hasESP = true
                        break
                    end
                end
                
                if not hasESP then
                    CreateGunESP(obj)
                end
            else
                -- If it's held, remove ESP
                RemoveGunESP(obj)
            end
        end
    end
    
    -- Clean up ESP for guns that no longer exist
    for i = #gunESPObjects, 1, -1 do
        local esp = gunESPObjects[i]
        if not esp.Tool or not esp.Tool.Parent then
            if esp.Billboard then esp.Billboard:Destroy() end
            table.remove(gunESPObjects, i)
        end
    end
end

-- ===== TOGGLE FUNCTIONS =====
local function ToggleESP()
    espEnabled = not espEnabled
    
    if espEnabled then
        for _, esp in pairs(espObjects) do
            if esp.Highlight then esp.Highlight:Destroy() end
        end
        espObjects = {}
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                CreateGlowESP(player)
            end
        end
        
        espConnection = RunService.RenderStepped:Connect(function()
            if espEnabled then
                UpdateGlowESP()
            end
        end)
        
        playerAddedConnection = Players.PlayerAdded:Connect(function(player)
            if espEnabled then
                task.wait(0.5)
                CreateGlowESP(player)
            end
        end)
        
        playerRemovedConnection = Players.PlayerRemoved:Connect(function(player)
            if espEnabled then
                RemoveGlowESP(player)
            end
        end)
        
        local function onCharacterAdded(player)
            if espEnabled then
                task.wait(0.5)
                RemoveGlowESP(player)
                CreateGlowESP(player)
            end
        end
        
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                player.CharacterAdded:Connect(function()
                    onCharacterAdded(player)
                end)
            end
        end
        
        characterAddedConnection = Players.PlayerAdded:Connect(function(player)
            player.CharacterAdded:Connect(function()
                onCharacterAdded(player)
            end)
        end)
        
        print("✅ Glow ESP Enabled")
    else
        if espConnection then espConnection:Disconnect() end
        if playerAddedConnection then playerAddedConnection:Disconnect() end
        if playerRemovedConnection then playerRemovedConnection:Disconnect() end
        if characterAddedConnection then characterAddedConnection:Disconnect() end
        
        for _, esp in pairs(espObjects) do
            if esp.Highlight then esp.Highlight:Destroy() end
        end
        espObjects = {}
        print("❌ Glow ESP Disabled")
    end
end

local function ToggleGunESP()
    gunESPEnabled = not gunESPEnabled
    
    if gunESPEnabled then
        -- Clear existing gun ESP
        for _, esp in pairs(gunESPObjects) do
            if esp.Billboard then esp.Billboard:Destroy() end
        end
        gunESPObjects = {}
        
        -- Initial scan
        ScanForDroppedGuns()
        
        -- Scan every frame
        gunScanConnection = RunService.RenderStepped:Connect(function()
            if gunESPEnabled then
                ScanForDroppedGuns()
            end
        end)
        
        -- Also scan when new objects are added
        toolAddedConnection = Workspace.DescendantAdded:Connect(function(child)
            if gunESPEnabled then
                if child:IsA("Tool") and child.Name == "Gun" then
                    task.wait(0.1)
                    ScanForDroppedGuns()
                end
            end
        end)
        
        print("🔫 Gun ESP Enabled - Will show dropped guns!")
    else
        if gunScanConnection then gunScanConnection:Disconnect() end
        if toolAddedConnection then toolAddedConnection:Disconnect() end
        
        for _, esp in pairs(gunESPObjects) do
            if esp.Billboard then esp.Billboard:Destroy() end
        end
        gunESPObjects = {}
        print("❌ Gun ESP Disabled")
    end
end

-- ===== MOVEMENT FUNCTIONS =====
local function ApplyMovement()
    local character = LocalPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end
    
    if jumpEnabled then
        humanoid.JumpPower = jumpPower
    else
        humanoid.JumpPower = originalJumpPower
    end
    
    if speedEnabled then
        humanoid.WalkSpeed = walkSpeed
    else
        humanoid.WalkSpeed = originalWalkSpeed
    end
end

local function ToggleJump()
    jumpEnabled = not jumpEnabled
    if jumpEnabled then
        print("✅ Jump Power: " .. jumpPower)
    else
        print("❌ Jump Power reset to: " .. originalJumpPower)
    end
    ApplyMovement()
end

local function ToggleSpeed()
    speedEnabled = not speedEnabled
    if speedEnabled then
        print("✅ Walk Speed: " .. walkSpeed)
    else
        print("❌ Walk Speed reset to: " .. originalWalkSpeed)
    end
    ApplyMovement()
end

-- Watch for character changes
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    ApplyMovement()
end)

-- ===== SIMPLE AIM FUNCTIONS =====
local function GetMurderer()
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer then
            if v.Backpack:FindFirstChild("Knife") or (v.Character and v.Character:FindFirstChild("Knife")) then
                return v
            end
        end
    end
    return nil
end

local function EquipGun()
    local gun = LocalPlayer.Backpack:FindFirstChild("Gun")
    if gun then
        LocalPlayer.Character.Humanoid:EquipTool(gun)
        return gun
    end
    return LocalPlayer.Character:FindFirstChild("Gun")
end

local function ShootMurderer()
    local murderer = GetMurderer()
    if not murderer or not murderer.Character then return end

    local targetHRP = murderer.Character:FindFirstChild("HumanoidRootPart")
    local myHRP = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

    if not targetHRP or not myHRP then return end

    local gun = EquipGun()
    if not gun then return end

    local shootRemote = gun:FindFirstChild("Shoot")
    if not shootRemote then return end

    local velocity = targetHRP.AssemblyLinearVelocity
    local predicted = targetHRP.Position + (velocity * prediction)
    predicted = predicted + Vector3.new(0, verticalOffset, 0)

    local args = {
        CFrame.new(myHRP.Position, predicted),
        CFrame.new(predicted)
    }
    shootRemote:FireServer(unpack(args))
end

-- ===== AUTO-SHOOT =====
local function AutoShootLoop()
    if not autoShoot then return end
    
    local murderer = GetMurderer()
    if murderer then
        local targetPos = murderer.Character:FindFirstChild("HumanoidRootPart")
        if targetPos then
            local screenPos, onScreen = Camera:WorldToViewportPoint(targetPos.Position)
            if onScreen then
                local distance = (Vector2.new(screenPos.X, screenPos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if distance < fovRadius then
                    ShootMurderer()
                    task.wait(math.random(150, 350) / 1000)
                end
            end
        end
    end
end

-- ===== MOBILE GUI =====
local function CreateMobileGUI()
    if mobileGUI then mobileGUI:Destroy() end
    
    mobileGUI = Instance.new("ScreenGui")
    mobileGUI.Name = "MM2MobileGUI"
    mobileGUI.ResetOnSpawn = false
    mobileGUI.IgnoreGuiInset = true
    mobileGUI.Parent = LocalPlayer.PlayerGui
    
    buttonFrame = Instance.new("Frame")
    buttonFrame.Size = UDim2.new(0, 120, 0, 120)
    buttonFrame.Position = UDim2.new(0.85, -60, 0.75, -60)
    buttonFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    buttonFrame.BackgroundTransparency = 0.3
    buttonFrame.BorderSizePixel = 2
    buttonFrame.BorderColor3 = Color3.fromRGB(255, 255, 255)
    buttonFrame.Parent = mobileGUI
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = buttonFrame
    
    local buttonLabel = Instance.new("TextLabel")
    buttonLabel.Size = UDim2.new(1, 0, 1, 0)
    buttonLabel.BackgroundTransparency = 1
    buttonLabel.Text = "🔫"
    buttonLabel.TextSize = 50
    buttonLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    buttonLabel.Font = Enum.Font.SourceSansBold
    buttonLabel.Parent = buttonFrame
    
    local button = Instance.new("ImageButton")
    button.Size = UDim2.new(1, 0, 1, 0)
    button.BackgroundTransparency = 1
    button.Parent = buttonFrame
    
    button.MouseButton1Click:Connect(function()
        if autoShoot then
            print("Auto-shoot is on! Toggle off for manual.")
        else
            ShootMurderer()
        end
    end)
    
    local dragging = false
    local dragStart, startPos
    
    button.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = buttonFrame.Position
        end
    end)
    
    button.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
    
    button.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.Touch then
            local delta = input.Position - dragStart
            local viewportSize = game:GetService("GuiService"):GetScreenResolution()
            buttonFrame.Position = UDim2.new(
                startPos.X.Scale + (delta.X / viewportSize.X),
                0,
                startPos.Y.Scale + (delta.Y / viewportSize.Y),
                0
            )
        end
    end)
end

-- ===== RAYFIELD UI =====

-- Main Tab
MainTab:CreateToggle({
    Name = "Glow ESP",
    CurrentValue = false,
    Callback = function(Value)
        ToggleESP()
    end
})

MainTab:CreateToggle({
    Name = "Gun ESP (Dropped Guns)",
    CurrentValue = false,
    Callback = function(Value)
        ToggleGunESP()
    end
})

MainTab:CreateToggle({
    Name = "Auto-Shoot",
    CurrentValue = false,
    Callback = function(Value)
        autoShoot = Value
        if Value then
            print("✅ Auto-shoot enabled!")
            spawn(function()
                while autoShoot do
                    AutoShootLoop()
                    task.wait(0.05)
                end
            end)
        else
            print("❌ Auto-shoot disabled")
        end
    end
})

MainTab:CreateKeybind({
    Name = "Manual Shoot (Q)",
    CurrentKeybind = "Q",
    HoldToInteract = false,
    Callback = function()
        if not autoShoot then
            ShootMurderer()
        end
    end
})

-- Movement Tab
MovementTab:CreateToggle({
    Name = "Super Jump",
    CurrentValue = false,
    Callback = function(Value)
        ToggleJump()
    end
})

MovementTab:CreateSlider({
    Name = "Jump Power",
    Range = {30, 200},
    Increment = 5,
    Suffix = "",
    CurrentValue = 50,
    Callback = function(Value)
        jumpPower = Value
        if jumpEnabled then
            ApplyMovement()
            print("✅ Jump Power set to: " .. jumpPower)
        end
    end
})

MovementTab:CreateToggle({
    Name = "Super Speed",
    CurrentValue = false,
    Callback = function(Value)
        ToggleSpeed()
    end
})

MovementTab:CreateSlider({
    Name = "Walk Speed",
    Range = {16, 100},
    Increment = 1,
    Suffix = "",
    CurrentValue = 16,
    Callback = function(Value)
        walkSpeed = Value
        if speedEnabled then
            ApplyMovement()
            print("✅ Walk Speed set to: " .. walkSpeed)
        end
    end
})

-- Settings Tab
SettingsTab:CreateSlider({
    Name = "Prediction Time",
    Range = {0.1, 0.3},
    Increment = 0.005,
    Suffix = "s",
    CurrentValue = 0.185,
    Callback = function(Value)
        prediction = Value
    end
})

SettingsTab:CreateSlider({
    Name = "Vertical Offset",
    Range = {0.3, 2.0},
    Increment = 0.05,
    Suffix = "studs",
    CurrentValue = 0.8,
    Callback = function(Value)
        verticalOffset = Value
    end
})

SettingsTab:CreateSlider({
    Name = "Auto-Shoot FOV",
    Range = {50, 500},
    Increment = 10,
    Suffix = "px",
    CurrentValue = 300,
    Callback = function(Value)
        fovRadius = Value
    end
})

-- ===== INITIALIZATION =====
if isMobile then
    CreateMobileGUI()
    print("📱 Mobile mode detected!")
end

-- Save settings when changed
Window:SaveConfiguration()

print("✅ Script Loaded!")
print("🔴 Murderer | 🔵 Sheriff | 🟢 Innocent")
print("🔫 Gun ESP - Shows dropped guns with pulsing markers")
print("🦘 Super Jump & Speed in Movement tab")
print("💾 Settings auto-save when changed!")
